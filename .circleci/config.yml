version: 2

jobs:
  test:
    machine: true
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: npm install
      - run: npm run setup
      - run: npm test

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
